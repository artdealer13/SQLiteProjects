# 💼 Задание 3: Бюджетный планировщик с аналитикой

## Описание
Система учета личных финансов с продвинутыми возможностями: управлением категориями, аналитикой, регулярными транзакциями и финансовыми целями.

## Возможности

### ✅ Реализовано:

#### **Часть 1: Базовый функционал**

1. **CRUD для транзакций**
   - ➕ Создание транзакций с обязательной категоризацией
   - 📝 Редактирование (с использованием транзакций БД)
   - ❌ Удаление (с использованием транзакций БД)

2. **Управление иерархией категорий**
   - 📂 Древовидная структура категорий
   - 🌳 Рекурсивные запросы WITH RECURSIVE
   - Поддержка подкатегорий

3. **Бюджетирование**
   - 📊 Плановые бюджеты на месяц
   - 💰 Соотношение: План vs Факт

#### **Часть 2: Аналитика и отчеты**

4. **Отчеты за период**
   - 📈 Детализация доходов/расходов
   - 🎯 Баланс за период

5. **Динамика по месяцам**
   - 📊 Помесячная статистика по доходам/расходам
   - Анализ тренда

6. **Сравнение с предыдущими периодами**
   - 📊 Сравнение месяца с месяцем

7. **Анализ превышения бюджета**
   - ⚠️ Выявление переполнения категорий
   - Процент использования

8. **Прогнозирование накоплений**
   - 📈 Прогноз реализации целей
   - 🔮 На основе среднего дохода

#### **Часть 3: Продвинутые функции**

7. **Регулярные транзакции**
   - 🔄 Автоматическое создание (daily/weekly/monthly)
   - 📅 Отслеживание последнего выполнения

8. **Система финансовых целей**
   - 🎯 Визуализация прогресса (прогрессбар)
   - 🔮 Прогнозирование достижения
   - 🏆 Приоритизация

9. **Оптимизация запросов**
   - 🚀 Индексы на часто используемых полях:
     - `idx_transactions_date` - на дату транзакций
     - `idx_transactions_category` - на категорию
     - `idx_budgets_month` - на месяц бюджета
     - `idx_categories_type` - на тип категории

## Структура БД

```sql
-- Иерархия категорий
CREATE TABLE categories (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    parent_id INTEGER,
    category_type TEXT CHECK(category_type IN ('income', 'expense')),
    description TEXT,
    FOREIGN KEY (parent_id) REFERENCES categories(id)
)

-- Транзакции
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY,
    category_id INTEGER NOT NULL,
    amount REAL NOT NULL,
    transaction_date TEXT NOT NULL,
    description TEXT,
    FOREIGN KEY (category_id) REFERENCES categories(id)
)

-- Бюджеты
CREATE TABLE budgets (
    id INTEGER PRIMARY KEY,
    category_id INTEGER NOT NULL,
    month TEXT NOT NULL,
    planned_amount REAL NOT NULL,
    UNIQUE(category_id, month)
)

-- Финансовые цели
CREATE TABLE financial_goals (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    target_amount REAL NOT NULL,
    current_amount REAL DEFAULT 0,
    target_date TEXT NOT NULL,
    priority TEXT DEFAULT 'normal'
)

-- Регулярные транзакции
CREATE TABLE recurring_transactions (
    id INTEGER PRIMARY KEY,
    category_id INTEGER NOT NULL,
    amount REAL NOT NULL,
    description TEXT,
    frequency TEXT NOT NULL,
    last_executed TEXT,
    is_active INTEGER DEFAULT 1
)
```

## Используемые SQL функции

### Рекурсивные запросы
```sql
-- Иерархия категорий с WITH RECURSIVE
WITH RECURSIVE category_tree AS (
    SELECT id, name, parent_id, category_type, 0 as level
    FROM categories
    WHERE parent_id IS NULL
    
    UNION ALL
    
    SELECT c.id, c.name, c.parent_id, c.category_type, ct.level + 1
    FROM categories c
    JOIN category_tree ct ON c.parent_id = ct.id
)
```

### JOIN и агрегирующие функции
```sql
-- Анализ бюджета
SELECT 
    c.name,
    COALESCE(b.planned_amount, 0) as budget,
    COALESCE(SUM(t.amount), 0) as spent
FROM categories c
LEFT JOIN budgets b ON c.id = b.category_id
LEFT JOIN transactions t ON c.id = t.category_id
GROUP BY c.id
```

### Работа с датами и функции (DATE, julianday)
```sql
-- Регулярные транзакции
WHERE strftime('%Y-%m', t.transaction_date) = ?
```

### Оконные функции (ROW_NUMBER)
```sql
-- Используется для выделения последовательностей (когда будут реализованы серии)
ROW_NUMBER() OVER (ORDER BY date) - 
ROW_NUMBER() OVER (PARTITION BY type ORDER BY date)
```

## Использование

```bash
cd task3_budget
python3 budget_planner.py
```

## Примеры функциональности

### Создание иерархии категорий
```
Меню → 1. Категории → 1. Добавить
Название: Еда
Тип: expense

Название: Ресторан
Тип: expense
ID родителя: 1 (Еда)
```

### Добавление транзакции в транзакции БД
```python
# Используется BEGIN TRANSACTION для согласованности
self.conn.execute("BEGIN TRANSACTION")
# ...гарантирует, что либо всё сохранится, либо ничего
self.conn.commit()
```

### Анализ бюджета
```
Меню → 3. Бюджеты → 2. Анализ
Месяц: 2026-02
Показывает процент использования каждой категории
```

### Прогнозирование целей
```
Меню → 4. Цели → 3. Прогноз
ID цели: 1
- Рассчитывает среднее ежемесячное сбережение
- Прогнозирует достижение цели к целевой дате
```

## Тестовые данные

Включают:
- 6 категорий (доходы и расходы)
- 30 дней транзакций
- 2 финансовые цели
- 3 установленные бюджеты

Загрузить через меню: "7. 🚀 Добавить тестовые данные"

## Технические особенности

### ✓ Транзакции БД
- Используются для безопасного обновления и удаления
- BEGIN TRANSACTION / COMMIT / ROLLBACK

### ✓ Индексы для оптимизации
- Индекс на дату (быстрый фильтр)
- Индекс на категорию (JOIN оптимизация)
- Индекс на месяц (период запросы)
- Индекс на тип категории (фильтр доходы/расходы)

### ✓ Рекурсивные запросы
- Иерархия категорий с любой глубиной
- Вывод уровня вложенности

### ✓ Агрегирующие функции
- COUNT, SUM, AVG для статистики
- COALESCE для обработки NULL

### ✓ Оконные функции
- ROW_NUMBER() OVER - готово для расширений

## Файлы проекта

- `budget_planner.py` - основное приложение (~700 строк)
- `budget.db` - база данных SQLite (создаётся автоматически)

## Возможные расширения (дополнительные баллы):

- ✅ Кэширование часто запрашиваемых данных
- ✅ REST API (Flask/FastAPI)
- ✅ Импорт CSV/Excel
- ✅ Шифрование чувствительных данных
- ✅ Веб-интерфейс с графиками
**Студент: Новихин Максим
## Статус: ✅ ЗАВЕРШЕНО
